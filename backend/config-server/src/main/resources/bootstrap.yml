spring 
  application
    name: config-server
  cloud
    config
      discovery
        enabled: true
        serviceId: discovery-server

eureka
  client
    serviceUrl
      defaultZone: http://${spring.cloud.config.discovery.serviceId}.default:8900/eureka
  instance:
    metadataMap:
      user: discovery
      password: Wuzr{ZJp4x}`svGU
